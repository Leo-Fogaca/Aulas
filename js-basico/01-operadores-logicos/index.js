let a = 3
let b = 3

// menor que ---- menor igual que

a < b; 
a <= b;

// maior que ---- maior igual que

a > b;
a >= b;

// igual ---- idêntico

a == b;
a === b;

// negação

a != b; 
a !== b;

// comparação com "E"

a >= b && a === b;

// comparação com "OU"

a >= b || a === b;

// NOT

!(a === b);